/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["i.ibb.co", "shahdab.vercel.app"], // Add other domains if needed
  },
};

module.exports = nextConfig;
